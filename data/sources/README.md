# Sources

Reusable provenance records use YAML and stable `source.*` IDs. The content pipeline discovers `.yaml` and `.yml` files recursively in this directory and validates every entity, assertion, and narrative reference against them.

Keep a source here when multiple content packages may cite it; package-specific editorial explanation belongs in the relevant Markdown instead.
